db:
  image: orchardup/postgresql
  ports:
    - "5432"
web:
  build: .
  command: python app.py
  ports:
   - "5000:5000"
  volumes:
   - .:/code
  links:
   - db
  environment:
    POSTGRESQL_USER=titles-service
    POSTGRESQL_PASS=password

