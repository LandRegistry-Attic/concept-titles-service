db:
  image: orchardup/postgresql
  ports:
    - "5432"
  environment:
    POSTGRESQL_USER: titles
    POSTGRESQL_PASS: password
    POSTGRESQL_DB: titles
web:
  build: .
  command: sh -c "sleep 7; python app.py"
  ports:
   - "5000:5000"
  volumes:
   - .:/code
  links:
   - db
